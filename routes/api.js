var app = require('../app');
var express = require('express');
var router = express.Router();

// This will define routes for accessing the information we will collect
router.get('/', function(req, res) {
  console.log("Nothing here...");
});
